// this file was created by terminal prompt: npm init -- then following the questions and creating it in the pwd
{
    "name": "gulpflow",
    "version": "0.0.1",
    "description": "A project to showcase a git, gulp, browserify workflow",
    "main": "index.js", // or app.js; change it to whatever
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    }, // there won't be tests for this
    "repository": {
        "type": "git",
        "url": "?.git" // this is because I have not created a url for the repo yet.
    },
    "author": "Will J. Haggard"
}